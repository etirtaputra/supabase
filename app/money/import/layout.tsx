export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Import Transactions | Money Manager',
};

export default function ImportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
